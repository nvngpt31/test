#!/bin/bash
  clear
  echo "Running Puppet locally"
  echo "" 
  echo "`date`"
  echo "" 
  echo "" 
  puppet apply --environment=production --modulepath=/etc/puppet/environments/production/modules/ -e 'include ucm::legacy::product' 
  # puppet apply --environment=production --modulepath=/etc/puppet/environments/production/modules/ -e 'include ucm::legacy' 
  echo "" 
  echo "" 

