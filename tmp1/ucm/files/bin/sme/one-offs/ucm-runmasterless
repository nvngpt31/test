#!/bin/bash

  if [[ -z $1 ]] 
  then echo "... PLEASE PROVIDE CLASS NAME TO RUN LOCALLY"
       exit 1
  fi 

  class=$1

  mpath='/etc/puppet/environments/production/modules'
  puppet apply --modulepath=${mpath} -e "include ${class}"
