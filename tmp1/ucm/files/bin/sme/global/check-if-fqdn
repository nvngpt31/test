#!/bin/bash

  if [[ -z $1 ]] 
  then echo '... PLEASE PROVIDE NODE NAME TO CHECK'
       exit 1
  fi 

  domain="`echo $1 | awk -F '.' '{print $2}'`"
  if [[ -z $domain ]]
  then echo "... $1 NOT AN FQDN"  
       exit 1
  else exit 0
  fi 
