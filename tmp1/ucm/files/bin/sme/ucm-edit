#!/bin/bash 
  
  alias fmt='echo ""'
  alias fmtline='echo "--------------------------------------------------------"'

  check_gitdir () {
    git rev-parse --git-dir >/dev/null 2>&1
    if [[ $? != 0 ]] ; then echo "... NOT IN A GIT DIRECTORY, BAILING." ; exit 1 ; fi
  }
  
