---

# This is really for legacy stuff.  We already set defaults in the defined types themselves.
legacy::precommands:
  'test -d /tmp/null':
    unless: 'test -d /var'

legacy::commands:
  'test -d /tmp':
    unless: 'test -d /var'
 
