FileLocation.configFileRoot=/app/coherence/trading-grid/config
FileLocation.configFile=/app/coherence/trading-grid/config/application.xml
DirectoryService.URL=<%= @cns_config['url'] %>
DirectoryService.Backup=<%= @cns_config['backup'] %>
DirectoryService.Override=<%= @cns_config['override'] %>
# this property should be moved somewhere as it is not related to CNS it was placed here as lack of TG to have a
# properties file of its own.
com.tdameritrade.trading.automation.notification.retryEnabled=<%= @cns_config['retry_enabled'] %>
